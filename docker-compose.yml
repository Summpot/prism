services:
  prism:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PRISM_CONFIG: /etc/prism/prism.toml
    volumes:
      - ./path/to/prism.toml:/etc/prism/prism.toml:ro
    ports:
      - "8080:8080" # Admin API
      - "7000:7000" # tunnel endpoint
